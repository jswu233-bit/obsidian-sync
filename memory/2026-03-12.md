# 2026-03-12

## Dashboard / Monitor 决策与落地
- Jamie 明确要求：看板应反映 **Zoe + subagents（尤其 Ops/Spy/Joy）运行状态**，而不是库存页或静态假数据。
- `monitor.html` 方向改为运行态监控，并强调必须是“可验证数据源”。
- 访问问题复盘：`localhost` 在 Discord/跨设备场景易误导；需要优先提供局域网地址（如 `http://192.168.31.212:9000/...`）。

## 心跳策略（关键配置）
- Jamie 要求给 Ops 开心跳：**55m**。
- Ops 心跳职责：检查 Claude 与 GPT 模型连通性；异常时主动提醒 Jamie。
- 已通过网关配置补丁启用 Ops heartbeat（55m）并保留/恢复 main(Zoe) heartbeat。

## UI/风格偏好
- Jamie 对“参考图一致性”要求高，明确指出“差别太大”；后续 UI 改造需优先贴近参考布局与视觉层级。
- 偏好：主控中心风格（左侧导航 + 中间总览 + 右侧状态）而非简陋轻量页。

## OpenClaw Control Center 安装结果
- 按 Jamie 提供的安装 prompt 执行了 control-center 接入流程。
- 首次克隆目录 `openclaw-control-center` 出现不完整（缺 `src/runtime`）；按规范重拉到 `control-center/` 后结构完整。
- `.env` 使用安全默认（readonly/auth 等）并接到本机网关：`ws://127.0.0.1:18789`，`OPENCLAW_HOME=/Users/jamiewu/.openclaw`，`UI_PORT=4310`。
- 验证结果：
  - `npm run build` 通过。
  - `npm test` 有 1 项失败（缺 `docs/mission.runbook.md`，仓库内容问题）。
  - `npm run smoke:ui` 失败（脚本检查口径问题），但 UI 可访问。
- 对 Jamie 提供可本机访问地址：`http://localhost:4310/?section=overview&lang=zh`（以及局域网地址可用）。
