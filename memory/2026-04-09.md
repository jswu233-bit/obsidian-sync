- 用户确认 nano-banana-pro 的新网关地址应为 `https://lnapi.com/`，旧地址 `nb.wuaiapi.com:3000` 已废弃。
- 今日修复了 `~/.openclaw/skills/nano-banana-pro/`：将 `scripts/generate_image.py` 从 Google GenAI SDK 调用改为 lnapi 的 OpenAI 兼容图片接口 `POST /v1/images/generations`。
- 同步更新了 `nano-banana-pro/SKILL.md`，明确 lnapi 走 OpenAI-compatible images API，而不是 Google GenAI SDK。
- 本次排障确认：`uv` 原先未安装，已完成安装；之后 NANO 的主要问题转为 provider 路由/模型兼容，而非本机依赖缺失。
## 2026-04-09
- Jamie 要求：日报缺失时，优先直接补发 markdown 正文到 Discord #daily，而不是只给文件路径或 commit。
- Jamie 要求：把“下次直接发日报 md”纳入日报 SOP，作为默认交付方式之一。
- 截至 2026-04-08 晚，已确认最新日报为 `daily/2026-04-07-update.md`；缺失区间为 `2026-04-02` 至 `2026-04-06` 共 5 天，Jamie 回复“继续”，表示继续补缺失日报。
