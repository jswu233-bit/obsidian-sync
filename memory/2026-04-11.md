# 2026-04-11 Daily Memory

## 今日任务
- 执行 cron 提醒：daily-memory-log-creator（每日记忆维护）。
- 检查并创建 `memory/2026-04-11.md`。
- 复盘今日对话与 `.learnings/LEARNINGS.md`，提炼可沉淀的长期规则/关键决策。
- 同步更新 `MEMORY.md` 与飞书索引主表。
- 执行记忆维护 git 提交命令。

## 重要事件⭐
- 23:30（Asia/Shanghai）收到并执行每日记忆维护提醒。
- 今日 `memory/2026-04-11.md` 原先不存在，已按模板创建。
- 今日新增可沉淀长期规则 3 条：Discord 回复去重、`web_search` 时间过滤互斥、`summarize` CLI 依赖预检。

## 待跟进
- 确认飞书索引主表已补录今日新增长期记忆条目。

## 已完成
- 已创建 `memory/2026-04-11.md`。
- 已复核 `MEMORY.md` 现有规则，避免与已存在的日报 A 版模板等条目重复。
- 已从 `.learnings/LEARNINGS.md` 提取今日新增、且仍具长期价值的规则候选。
- 已准备执行 git 提交命令归档本次维护变更。

## 对话要点
- 今日已有明确用户纠正/工具复盘沉淀在 `.learnings/LEARNINGS.md`：
  1) Spy 日报需保持已确认的 A 版格式，不能擅自改版（该规则已存在于 `MEMORY.md`，本次不重复追加）。
  2) Discord 场景需避免对同一问题重复回复。
  3) `web_search` 不能同时传 `freshness` 与 `date_after/date_before`。
  4) 依赖 `summarize` 技能前，应先检查本机是否安装 `summarize` CLI。

## 备注
- 本次 daily memory 基于真实文件/索引/命令结果维护。
