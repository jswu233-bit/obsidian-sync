# 2026-04-17

## Durable memories

- 用户再次明确要求：Discord 回复要**简洁直接**，先给结论，少解释；除非用户追问，否则不要展开成长回复。
- Discord 群聊需要**严格去重**：同一问题/同一轮重复投递，若已答过，应优先静默，不要再次重复作答。
- 关于跨 agent 共用 skills 的推荐结构已收敛为：把 skill 本体统一放在 `~/.openclaw/skills-src/`（或同类 canonical source 目录），再 symlink 到 `~/.claude/skills/`、`~/.codex/skills/`、`~/.openclaw/skills/`；不建议把源目录直接和某个 agent 的安装目录混用。
- 关于 skill 体系的概念分层，已形成稳定说法：**SOP = 编排层，Skill = 能力模块，Script = 执行层**。Skill 可以做轻编排，但强流程编排仍以 SOP 更合适。
